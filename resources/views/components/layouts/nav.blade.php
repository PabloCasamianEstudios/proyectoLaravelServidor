<div class="navbar bg-base-100 flex content-center justify-around">
        <a class="btn btn-ghost text-xl" href="{{route('home')}}">Inicio</a>
    <a class="btn btn-ghost text-xl" href="{{route('about')}}">Sobre el club</a>
    <a class="btn btn-ghost text-xl" href="{{route('news')}}">Novedades</a>
</div>
